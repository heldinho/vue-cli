const mutations = {
  SET_DRAWER_CLIENT: (state, payload) => {
    state.stateDrawerClient = payload
  },
  SET_DATA_CLIENT: (state, payload) => {
    state.stateDataClient = payload
  }
}

export default mutations
